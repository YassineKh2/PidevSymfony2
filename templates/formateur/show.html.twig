{% extends 'baseBack.html.twig' %}

{% block title %}Formateur{% endblock %}

{% block body %}
    <h1>Formateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formateur.id }}</td>
            </tr>
            <tr>
                <th>NomFormateur</th>
                <td>{{ formateur.NomFormateur }}</td>
            </tr>
            <tr>
                <th>PrenomFormateur</th>
                <td>{{ formateur.PrenomFormateur }}</td>
            </tr>
            <tr>
                <th>SexeFormateur</th>
                <td>{{ formateur.SexeFormateur }}</td>
            </tr>
            <tr>
                <th>EmailFormateur</th>
                <td>{{ formateur.EmailFormateur }}</td>
            </tr>
            <tr>
                <th>NumTelFormateur</th>
                <td>{{ formateur.NumTelFormateur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formateur_index') }}">back to list</a>

    <a href="{{ path('app_formateur_edit', {'id': formateur.id}) }}">edit</a>

    {{ include('formateur/_delete_form.html.twig') }}
{% endblock %}
