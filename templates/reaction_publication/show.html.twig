{% extends 'base.html.twig' %}

{% block title %}ReactionPublication{% endblock %}

{% block body %}
    <h1>ReactionPublication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reaction_publication.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reaction_publication_index') }}">back to list</a>

    <a href="{{ path('app_reaction_publication_edit', {'id': reaction_publication.id}) }}">edit</a>

    {{ include('reaction_publication/_delete_form.html.twig') }}
{% endblock %}
