{% extends 'baseBack.html.twig' %}

{% block body %}
<div class="content-body">
    <!-- row -->
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-8 col-lg-8 col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h4 class="card-title">Planning Centre</h4>
                        <br><br><br>
                    </div>
    <div align="center">
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <br>

            <div class="table-responsive">
                <table class="table table-striped">
                    <tbody>

            <tr>
                <th>Id</th>
                <td>{{ planning_centre.id }}</td>
            </tr>
            <tr>
                <th>DateDebutPlanning</th>
                <td>{{ planning_centre.DateDebutPlanning ? planning_centre.DateDebutPlanning|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFinPlanning</th>
                <td>{{ planning_centre.DateFinPlanning ? planning_centre.DateFinPlanning|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ planning_centre.Description }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ planning_centre.Titre }}</td>
            </tr>
        </tbody>
    </table>
                <button class="btn btn-primary mr-2">
    <a href="{{ path('app_planning_centre_index') }}">Retour</a>
    <button class="btn btn-primary mr-2"><a href="{{ path('app_planning_centre_edit', {'id': planning_centre.id}) }}">Edit</a></button>
   <br/>
                    <br/>

    {{ include('planning_centre/_delete_form.html.twig') }}

                </div>
            </div>
        </div>
    </div>
</div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
