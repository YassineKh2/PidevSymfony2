{{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
{{ form_errors(form.ContenuCommantaire) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Add') }}</button>
{{ form_end(form) }}
