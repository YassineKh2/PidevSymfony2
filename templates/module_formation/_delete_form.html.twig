<form method="post" action="{{ path('app_module_formation_delete', {'id': module_formation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <button href="{{ path('app_module_formation_delete', {'id': module_formation.id}) }}" title="Supprimer" class="fa fa-trash-o color-danger border-0" >
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ module_formation.id) }}">
    </button>
</form>
