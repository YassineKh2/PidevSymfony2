<form method="post" action="{{ path('app_activite_centre_delete', {'id': activite_centre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activite_centre.id) }}">
    <button class="btn btn-primary mr-2">Delete</button>
</form>
