<form method="post" action="{{ path('app_formateur_delete', {'id': formateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <button href="{{ path('app_formateur_delete', {'id': formateur.id}) }}" title="Supprimer" class="fa fa-trash-o color-danger border-0" >
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formateur.id) }}">
    </button>

</form>
